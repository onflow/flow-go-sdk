apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: flow
  name: emulator-storage-claim
  namespace: flow
spec:
  accessModes:
  - ReadWriteOnce
  dataSource: null
  resources:
    requests:
      storage: 100Gi
  storageClassName: standard
